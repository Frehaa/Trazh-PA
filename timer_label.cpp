#include "timer_label.h"

TimerLabel::TimerLabel(QWidget *parent = Q_NULLPTR, Qt::WindowFlags f = Qt::WindowFlags())
	: QLabel(parent, f)
{

}

//TimerLabel::TimerLabel(Timer timer, QWidget *parent = Q_NULLPTR, Qt::WindowFlags f = Qt::WindowFlags())
//{}

TimerLabel::~TimerLabel()
{}